<!doctype html>
<html lang="{{_base.language.code}}">

<head>
    <meta charset="utf-8" />
    <meta content="IE=edge" http-equiv="X-UA-Compatible" />
    <meta content="width=device-width, initial-scale=1" name="viewport" />
    <meta content="{{_base.description}}" name="description" />
    <meta content="SoftLoop" name="author" />
    <title>{{_base.title}}{{#if title}} - {{title}}{{/if}}</title>

    <meta content="{{_base.title}}" property="og:title">
    <meta content="{{_base.description}}" property="og:description">
    <meta content="{{_base.url}}" property="og:url">
    <meta content="{{_base.url}}/assets/img/logo_big.jpg" property="og:image">
    <meta content="product" property="og:type">

    {{#each _base.languages}}
        <link rel="alternate" hreflang="{{@key}}" href="{{@root/_base/url}}/{{@key}}/{{@root/_page}}.html" />
    {{/each}}

    <style>
        .bodyloader {
            z-index: 10000;
            display: block;
            position: fixed;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            background: #EEE;
            opacity: 1;
            filter: alpha(opacity=100);
            -moz-transition: opacity .5s linear;
            -o-transition: opacity .5s linear;
            -webkit-transition: opacity .5s linear;
            transition: opacity .5s linear;
        }

        .bodyloader.bhidden {
            opacity: 0;
            filter: alpha(opacity=0);
        }

        .bodyloader .spinner {
            margin: -61px 0 0 -61px;
            left: 50%;
            top: 50%;
            font-size: 10px;
            position: absolute;
            text-indent: -9999em;
            border-top: 1.1em solid rgba(113, 113, 113, 0.2);
            border-right: 1.1em solid rgba(113, 113, 113, 0.2);
            border-bottom: 1.1em solid rgba(113, 113, 113, 0.2);
            border-left: 1.1em solid #A2A2A2;
            -webkit-transform: translateZ(0);
            -ms-transform: translateZ(0);
            transform: translateZ(0);
            -webkit-animation: load8 1.1s infinite linear;
            animation: load8 1.1s infinite linear;
        }

        .bodyloader .spinner,
        .bodyloader .spinner:after {
            border-radius: 50%;
            width: 10em;
            height: 10em;
        }

        @-webkit-keyframes load8 {
            0% {
                -webkit-transform: rotate(0deg);
                transform: rotate(0deg);
            }
            100% {
                -webkit-transform: rotate(360deg);
                transform: rotate(360deg);
            }
        }

        @keyframes load8 {
            0% {
                -webkit-transform: rotate(0deg);
                transform: rotate(0deg);
            }
            100% {
                -webkit-transform: rotate(360deg);
                transform: rotate(360deg);
            }
        }
    </style>

    <!--[if lt IE 9]>
        <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.3/html5shiv.min.js"></script>
        <script src="//cdnjs.cloudflare.com/ajax/libs/respond.js/1.4.2/respond.min.js""></script>

        <link href="//netdna.bootstrapcdn.com/respond-proxy.html" id="respond-proxy" rel="respond-proxy" />
        <link href="libs/cross-domain/respond.proxy.gif" id="respond-redirect" rel="respond-redirect" />
        <script src="libs/cross-domain/respond.proxy.js"></script>

      <![endif]-->
</head>

<body>
